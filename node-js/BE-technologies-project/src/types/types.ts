interface AddUserInput {
    name: string;
    email: string;
    // role: string;
}

interface User {
    id: string;
    name: string;
    email: string;
    // role: string;
}

export {
    AddUserInput,
    User
}